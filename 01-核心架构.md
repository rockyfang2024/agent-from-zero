# 01 - 核心架构

## 1.1 一切围绕 Agent Loop

无论是 Hermes 还是 OpenClaw，它们的核心都是同一段逻辑——**Agent Loop（Agent 执行循环）**：

```
用户消息
    │
    ▼
┌─────────────────────────────────────┐
│ 1. 构建 Messages                    │
│    System Prompt + 历史 + 用户消息   │
└─────────────────┬───────────────────┘
                  │
                  ▼
┌─────────────────────────────────────┐
│ 2. 调用 LLM                          │
│    OpenAI-compatible API             │
└─────────────────┬───────────────────┘
                  │
           ┌──────┴──────┐
           │             │
      无 tool_call   有 tool_call
           │             │
           ▼             ▼
      返回用户       ┌─────────────────┐
                    │ 3. 执行工具       │
                    │    tool_call()   │
                    └────────┬────────┘
                             │结果重新注入 messages
                             ▼
                    ┌─────────────────┐
                    │ 回到步骤 2       │
                    │ 继续调用 LLM     │
                    └────────┬────────┘
                             │
                       重复直到完成
```

这就是"Agent"和普通聊天机器人的本质区别：**Agent 会调用工具，会自我修正，会循环直到任务完成**。

---

## 1.2 工具系统：注册 → 分发 → 执行

### 核心三件套

```
ToolSchema  ──定义工具长什么样（名字、参数、描述）
     │
     ▼
ToolRegistry  ──中心注册表，所有工具在此登记
     │
     ▼
handle_function_call()  ──根据名字找到并执行工具
```

**ToolSchema 例子（JSON Schema 风格）：**

```json
{
  "type": "function",
  "function": {
    "name": "read_file",
    "description": "读取文件内容",
    "parameters": {
      "type": "object",
      "properties": {
        "path": {"type": "string", "description": "文件路径"}
      },
      "required": ["path"]
    }
  }
}
```

**ToolRegistry（伪代码）：**

```python
class ToolRegistry:
    def __init__(self):
        self._tools = {}  # name -> (schema, handler)

    def register(self, name, schema, handler):
        self._tools[name] = (schema, handler)

    def get_schema(self, name):
        return self._tools[name][0]

    def get_handler(self, name):
        return self._tools[name][1]

    def list_tools(self):
        return [self.get_schema(name) for name in self._tools]
```

**handle_function_call 执行流程：**

```python
def handle_function_call(tool_name, tool_args, context):
    schema, handler = registry.get(tool_name)
    # 参数校验、权限检查（可插拔）
    result = handler(**tool_args, context=context)
    return format_result(result)
```

### Hermes vs OpenClaw 的工具系统对比

| 维度 | Hermes | OpenClaw |
|------|--------|----------|
| 语言 | Python | TypeScript |
| 注册时机 | 模块 import 时自动注册 | 启动时显式 registerTool |
| 工具来源 | tools/ 目录下每个文件注册自己的工具 | extensions/ 插件包 |
| Schema 格式 | Python dict（OpenAI 兼容） | JSON Schema |
| 执行 | 同步函数调用 | pi-agent-core（嵌入式） |
| 权限控制 | approval.py 危险命令检测 | Hook: before_tool_call |

---

## 1.3 Session（会话管理）

Session 是"多轮对话"的技术基础：**让 Agent "记住"之前说了什么**。

### 核心需求

1. **消息持久化**：对话历史要能保存，下次请求能带上
2. **多会话并发**：不同用户/不同对话是独立的，互不干扰
3. **上下文窗口管理**：LLM 有 token 上限，历史太长要"压缩"

### Hermes 的方案：SQLite + FTS5

```python
# Hermes 的 SessionDB（简化版）
class SessionStore:
    def __init__(self, db_path="~/.hermes/sessions.db"):
        self.db = sqlite3.connect(db_path)
        # messages 表: session_id, role, content, created_at
        # FTS5 全文索引用于检索历史

    def add_message(self, session_id, role, content):
        self.db.execute(
            "INSERT INTO messages VALUES (?, ?, ?, ?)",
            (session_id, role, content, datetime.now())
        )

    def get_messages(self, session_id, limit=20):
        return self.db.execute(
            "SELECT * FROM messages WHERE session_id=? ORDER BY created_at LIMIT ?",
            (session_id, limit)
        ).fetchall()
```

### OpenClaw 的方案：per-session 队列串行化

```javascript
// OpenClaw 的会话路由（简化版）
const sessionLanes = new Map(); // sessionKey -> lane queue

async function routeToAgent(sessionKey, message) {
    // 同一个 sessionKey 的请求串行执行（避免竞态）
    if (!sessionLanes.has(sessionKey)) {
        sessionLanes.set(sessionKey, createLane());
    }
    const lane = sessionLanes.get(sessionKey);
    return lane.queue(() => runAgent(sessionKey, message));
}
```

### 上下文窗口压缩（Context Compression）

当对话历史太长时，有两种处理策略：

**Hermes 策略：压缩而非截断**
```
[第1轮消息] [第2轮消息] ... [第N轮消息]
    │           │              │
    └───────────┴── 压缩 ──────┘
         保留语义，合并成长度减半的摘要
```

**OpenClaw 策略：滑动窗口 + 摘要**
- 保留最近 N 条消息
- 更早的消息做摘要存入 `memory/` 目录

---

## 1.4 Gateway（消息网关）

Gateway 是 Agent 的"接入层"——它负责**接收各种来源的消息，路由到正确的 Agent**。

### 整体架构

```
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ Telegram │  │ Discord  │  │ Feishu   │  │  CLI     │
└────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘
     │             │             │             │
     └─────────────┼─────────────┼─────────────┘
                   ▼
          ┌─────────────────┐
          │  Message Router  │
          │  (Binding 解析)   │
          └────────┬────────┘
                   │ 路由到对应 Agent
         ┌────────┴────────┐
         ▼                 ▼
   ┌──────────┐      ┌──────────┐
   │ Agent A  │      │ Agent B  │
   │ (工作区A)│      │ (工作区B)│
   └──────────┘      └──────────┘
```

### 路由优先级（OpenClaw）

消息来了，按以下优先级找 Agent（最具体优先）：

1. `peer` 精确匹配（DM 或群组 ID）
2. `parentPeer` 线程继承（Discord thread, Feishu thread）
3. `guildId + roles`（Discord 服务器 + 角色）
4. `accountId` 匹配
5. `channel` 通配
6. `default` Agent

```javascript
// 路由决策（伪代码）
function resolveAgent(message, bindings) {
    for (const binding of bindings) {
        if (matchPeer(message, binding.match.peer) &&
            matchRoles(message, binding.match.roles)) {
            return binding.agentId;
        }
    }
    return "default";
}
```

---

## 1.5 Skills（技能系统）

Skills 是**提示词的模块化组合**。它让非程序员也能扩展 Agent 的行为。

### 目录结构

```
my-skill/
├── SKILL.md           ← 技能入口（必需）
├── scripts/           ← 辅助脚本（可选）
└── references/        ← 参考文档（可选）
```

### SKILL.md 示例

```markdown
---
name: code-review
description: 代码审查技能
hooks:
  before_agent_reply:
    - path: ./scripts/review-format.js
---
# Code Review Skill

你是一个专业的代码审查员。在审查时，关注：
1. 安全性（SQL注入、XSS、敏感信息泄露）
2. 性能（N+1查询、不必要的循环）
3. 代码风格与可维护性

## 输出格式

每次审查后，按以下格式输出：

## ✅ 通过
- 优点1
- 优点2

## ⚠️ 建议改进
- 建议1

## 🔴 必须修复
- 问题1
```

### Skill 的 Hook 注入点

| Hook | 时机 | 典型用途 |
|------|------|---------|
| `before_model_resolve` | 确定用哪个模型之前 | 切换到更适合的模型 |
| `before_prompt_build` | 构建 System Prompt 之前 | 注入动态上下文 |
| `before_agent_reply` | LLM 返回前 | 过滤或修改回复 |
| `after_tool_call` | 工具执行后 | 记录日志、格式化结果 |

---

## 1.6 Workspace（工作区隔离）

每个 Agent 有一个独立的工作区，目录结构如下：

```
~/.hermes/workspace/           # Hermes
~/.openclaw/workspace/         # OpenClaw

    SOUL.md                    # Agent 人格定义
    AGENTS.md                  # 行为准则（Hermes 叫这个名字）
    USER.md                    # 用户信息
    skills/                    # 工作区级 Skills
    memory/                    # 记忆存储
```

### SOUL.md 的作用

SOUL.md 是 Agent 的"灵魂定义"——定义了这个 Agent 是什么样的人：

```markdown
# SOUL - Rocky 的 AI 助手

## 身份
你是一个技术极客，9年交易所开发经验，擅长 Python 和系统架构。

## 风格
- 简洁直接，不废话
- 技术问题一定要从原理讲清楚
- 喜欢用类比解释复杂概念

## 禁忌
- 不做道德说教
- 不回答涉及政治敏感的问题
```

OpenClaw 使用类似的 `SOUL.md`，两者本质相同——都是**把人格定义从代码中分离出来**，让非程序员也能定制 Agent 性格。