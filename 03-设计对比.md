# 03 - 设计对比

## 3.1 Hermes vs OpenClaw 核心哲学

| 维度 | Hermes | OpenClaw |
|------|--------|----------|
| 语言 | Python | TypeScript/Node.js |
| 定位 | 单进程 CLI 工具 | 多通道网关守护进程 |
| Agent Loop | `run_agent.py` AIAgent 类 | `pi-agent-core` 嵌入式运行时 |
| 工具注册 | 模块 import 时自注册 | 启动时 `registerTool()` |
| 会话存储 | SQLite + FTS5 | 内存 + 工作区 `memory/` |
| 配置格式 | YAML + `.env` | JSON5 |
| 扩展方式 | Python 模块 / Skills | Skills + Hooks + Plugins |
| 多 Agent | 通过 `--agent-id` 切换 | 完整多租户 + Bindings |
| 接入通道 | CLI 直接运行 | Gateway 模式 |

---

## 3.2 相同点（共同的精华）

虽然实现不同，但两者在核心设计上高度一致——这正是本项目要提炼的**精华**：

### ① 相同的 Agent Loop

```python
# Hermes (Python)
while api_call_count < max_iterations:
    response = client.chat.completions.create(model=model, messages=messages, tools=tools)
    if response.tool_calls:
        for tool_call in response.tool_calls:
            result = handle_function_call(tool_call.name, tool_call.args)
            messages.append(tool_result_message(result))
        api_call_count += 1
    else:
        return response.content
```

```typescript
// OpenClaw (TypeScript) - 概念等价
while (iterations < maxIterations) {
  const response = await runPiAgent({ model, messages, tools });
  for (const event of response.events) {
    if (event.type === "tool") {
      const result = handleToolCall(event.name, event.args);
      messages.push(toolResultMessage(result));
    }
  }
  if (!response.needsMoreCalls) return response.content;
}
```

**本质都是**：发消息 → 收响应 → 有工具调工具 → 继续发 → 直到完成。

### ② 相同的工具抽象

```
ToolSchema (name, description, parameters)
    ↓
ToolRegistry.register(name, schema, handler)
    ↓
handle_function_call(name, args) → result
```

两边工具的 JSON Schema 格式完全兼容 OpenAI Tool Calling 协议。

### ③ 相同的 Workspace 概念

```
~/.hermes/workspace/          ~/.openclaw/workspace/
    SOUL.md                    SOUL.md        ← Agent 人格
    AGENTS.md                  AGENTS.md      ← 行为准则（Hermes 独有）
    USER.md                    USER.md        ← 用户信息
    skills/                    skills/        ← 技能目录
    memory/                    memory/        ← 记忆存储
```

### ④ 相同的 Skill 机制

SKILL.md 格式完全兼容，Skill 加载逻辑几乎等价——这意味着**一个 Skill 可以同时给 Hermes 和 OpenClaw 用**。

### ⑤ 相同的分层架构

```
Platform Layer（消息通道）
    ↓
Gateway / Router（路由、Session）
    ↓
Agent Loop（核心智能）
    ↓
Tool System（工具执行）
    ↓
Model Provider（LLM 调用）
```

---

## 3.3 差异对比与各自的权衡

### Agent Loop 实现

**Hermes：同步循环，单进程**
```
优点：简单直观，易调试，Python 生态丰富
缺点：多会话并发需要 threading/multiprocessing
```

**OpenClaw：嵌入式运行时，per-session 队列**
```
优点：天然多会话并发，Gateway 统一管理
缺点：引入了 pi-agent-core 依赖，调试更复杂
```

### 会话存储策略

**Hermes：SQLite + FTS5**
```
- 支持全文搜索（历史对话检索）
- 单文件，无服务依赖
- FTS5 索引让搜索极快
```

**OpenClaw：文件系统 + JSON**
```
- memory/ 目录存摘要
- 纯文本，可直接查看
- 大规模会话时文件 I/O 可能成为瓶颈
```

### 配置哲学

**Hermes：YAML + .env**
```yaml
# ~/.hermes/config.yaml
model: anthropic/claude-opus-4.6
tools:
  enabled: true
```
```bash
# ~/.hermes/.env
ANTHROPIC_API_KEY=sk-...
```
→ 环境变量和配置文件分离，密钥不进仓库

**OpenClaw：JSON5 + 插件配置**
```json
{
  "channels": { "telegram": { "token": "..." } },
  "agents": { "list": [...] }
}
```
→ 统一配置树，结构更清晰但嵌套深

---

## 3.4 总结：精华设计清单

如果你要从零实现一个 Agent 系统，必须掌握的**核心设计**（无论选 Hermes 还是 OpenClaw 路线）：

```
✅ 必要设计（没有就做不成 Agent）
├── Agent Loop          循环调用 LLM + 执行工具
├── Tool Registry       中心化工具注册与分发
├── Message History     维护对话历史（Session）
└── System Prompt       把人格/指令注入 LLM

✅ 重要设计（影响架构质量）
├── Context Compression     对话太长时的处理策略
├── Session Isolation       多会话隔离与并发
├── Error Recovery          LLM 调用失败的重试逻辑
└── Tool Schema             工具参数校验

✅ 扩展设计（让系统更有生命力）
├── Skill System         提示词模块化，非程序员可扩展
├── Workspace           把配置从代码分离出来
├── Hook System         在关键节点插入自定义逻辑
└── Multi-Agent Routing     多 Agent 路由
```

**本项目的目的**：让你理解这些设计是什么、为什么是这样、以及如何在代码层面实现它们。

理解这些之后，无论你看 Hermes 源码还是 OpenClaw 源码，都不再是"天书"——你能看到每一行代码背后的设计意图。