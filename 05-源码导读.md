# 05 - Hermes 源码导读

> 带着问题读源码。每个问题先自己想，再去看对应的代码行。

---

## 源码地图

```
hermes-agent/
├── run_agent.py          ← ⭐ 核心：Agent Loop，12000 行
├── model_tools.py        ← 工具编排：发现、调用、分发
├── tools/
│   └── registry.py       ← 工具注册中心
├── agent/
│   ├── prompt_builder.py ← System Prompt 组装
│   ├── context_compressor.py ← 上下文压缩
│   └── ...
├── hermes_cli/          ← CLI 接入层
└── gateway/              ← 多通道消息网关
```

**阅读顺序建议**：

```
第一层（先读懂框架）：
    run_agent.py 的 __init__ 和 run_conversation 的主循环结构

第二层（理解工具系统）：
    tools/registry.py → model_tools.py → 一个具体工具（如 file_tools.py）

第三层（理解周边系统）：
    prompt_builder.py → context_compressor.py → hermes_state.py
```

---

## run_agent.py：核心 Agent Loop

### Q1：Agent Loop 的"循环"在哪里？找到 `while` 关键字

**思考**：在 12000 行的 `run_agent.py` 里，Agent Loop 的主体是一个 `while True` 还是 `for` 循环？它用什么条件终止？

---

**答案**

在 `run_conversation` 函数里（line 8516 起），主循环是：

```python
# run_agent.py line ~8766+
# 注意：这是简化版，真正代码在 line ~8800-9500
while self.iteration_budget.remaining > 0:
    # 1. 构建请求消息
    # 2. 调用 LLM（_interruptible_api_call）
    # 3. 解析响应
    # 4. 有 tool_call → 执行工具 → 继续
    # 5. 没有 tool_call → 结束
```

**关键点**：

- **终止条件**：`iteration_budget.remaining > 0`（默认 90 次迭代）
- **不是 `while True`**——有预算上限，防止无限循环
- **每个 tool_call 算一次迭代**，`execute_code` 可以"退款"（`budget.refund()`）

**验证方法**：

```bash
grep -n "iteration_budget" /home/ubuntu/.hermes/hermes-agent/run_agent.py | head -20
```

---

### Q2：`_build_system_prompt` 在哪？它是怎么把 SOUL.md + AGENTS.md + USER.md 拼起来的？

**思考**：如果 SOUL.md 很长，System Prompt 会不会超长？

---

**答案**

在 `run_agent.py` 里搜索：

```bash
grep -n "_build_system_prompt\|SOUL.md\|AGENTS.md" /home/ubuntu/.hermes/hermes-agent/run_agent.py | head -30
```

实际在 `agent/prompt_builder.py` 里。核心逻辑（简化版）：

```python
# agent/prompt_builder.py 的 build_system_prompt()
def build_system_prompt(self, system_message=None):
    parts = []

    # 1. 身份（SOUL.md）
    soul = load_soul_md(workspace)
    if soul:
        parts.append(soul)

    # 2. 行为准则（AGENTS.md）
    agents = load_agents_md(workspace)
    if agents:
        parts.append(agents)

    # 3. 用户信息（USER.md）
    user = load_user_md(workspace)
    if user:
        parts.append(user)

    # 4. 环境提示（当前目录、平台）
    parts.append(build_environment_hints())

    # 5. 已启用 Skills 的提示词
    for skill in self.enabled_skills:
        parts.append(skill.prompt_fragment)

    # 6. 自定义 system_message（覆盖，不重复）
    if system_message:
        parts.insert(0, system_message)

    return "\n\n---\n\n".join(filter(None, parts))
```

**防超长机制**：

- `_build_system_prompt` 结果会被 token 估算
- 超过阈值时会在 `_check_compression_model_feasibility` 阶段报错或自动压缩

**关键设计问题**：为什么 `system_message` 要 `insert(0, ...)` 而不是 `append`？

→ 因为 `system_message` 通常是"临时指令"（如"你现在是一个翻译"），优先级最高，要放在最前面。

---

### Q3：消息是怎么构建的？找到 `api_messages` 的构造位置

**思考**：发送给 LLM 的 `messages` 数组里，System Prompt 放在哪个位置？

---

**答案**

在 `run_conversation` 的主循环里，每次 API 调用前都会构建 `api_messages`：

```python
# run_agent.py 约 line 8900+
# 构建发给 LLM 的消息数组
api_messages = []

# System prompt 在最前面
if active_system_prompt:
    api_messages.append({
        "role": "system",
        "content": active_system_prompt,
        # Anthropic Prompt Caching：标记缓存边界
        **self._cache_control_hint(active_system_prompt)
    })

# 对话历史（不含当前 user message）
api_messages.extend(conversation_history or [])

# 已执行工具的结果（tool role 消息）
api_messages.extend(self._pending_tool_messages)

# 当前 user message
api_messages.append({"role": "user", "content": user_message})
```

**为什么 System Prompt 在最前面？**
- Anthropic 的 Prompt Caching 要求 `system` 必须在 `messages[0]`
- 大部分模型也期望 system 在最前

---

### Q4：工具调用结果是怎么"注入回"循环的？

**思考**：工具执行完后，结果是直接返回给用户，还是再发给 LLM？

---

**答案**

工具结果作为 `tool` 角色的消息追加到 `messages`，然后**继续循环**：

```python
# run_agent.py 约 line 9100+
for tool_call in response.tool_calls:
    # 执行工具
    tool_result = handle_function_call(
        tool_call.function.name,
        json.loads(tool_call.function.arguments),
        task_id=effective_task_id
    )

    # 工具结果作为一条消息追加
    messages.append({
        "role": "tool",
        "tool_call_id": tool_call.id,
        "content": tool_result,        # 必须是字符串
        "name": tool_call.function.name,
    })
    # 注意：这里只是 append 到 messages
    # 下一轮循环才会把这条消息发给 LLM
```

**为什么 `content` 必须是字符串？**

LLM 的 `messages` 格式要求 `tool` 消息的 content 是字符串（错误详情也在字符串里）。`handle_function_call` 内部会把各种返回类型（dict、Exception 等）全部转换成字符串。

---

### Q5：`IterationBudget` 是什么？为什么不用简单的 `counter += 1`？

**思考**：如果用 `counter` 会有什么并发问题？

---

**答案**

`IterationBudget` 在 line 202-244，有线程锁保护：

```python
class IterationBudget:
    def __init__(self, max_total: int):
        self.max_total = max_total
        self._used = 0
        self._lock = threading.Lock()

    def consume(self) -> bool:
        """尝试消耗一次迭代，有上限"""
        with self._lock:              # ← 线程安全
            if self._used >= self.max_total:
                return False
            self._used += 1
            return True

    def refund(self) -> None:
        """退款（如 execute_code 不算迭代）"""
        with self._lock:
            if self._used > 0:
                self._used -= 1
```

**为什么需要锁？**

```
场景：subagent 和 parent agent 同时检查 budget
- subagent: remaining = 1
- parent agent: remaining = 1
- subagent consume() → remaining = 0
- parent agent consume() → remaining = 0
结果：两个 agent 都以为自己还有 budget，可能超限
```

用 `threading.Lock()` 保证 `check-and-increment` 原子操作。

**另一个设计**：subagent 继承 parent 的 budget，但不共享同一个 counter——而是 parent 创建 `IterationBudget`，subagent 通过参数传入。这让 parent 能追踪整个调用树的总消耗。

---

## tools/registry.py：工具注册中心

### Q6：工具是怎么"自注册"的？找到 `registry.register` 的调用位置

**思考**：为什么不需要在某个地方写 `register_tool("read_file", ...)` 这样的显式调用？

---

**答案**

工具模块在 `import` 时就完成了注册。以 `file_tools.py` 为例：

```python
# tools/file_tools.py  (简化)
from tools.registry import registry  # 导入全局注册表

def read_file_handler(path: str, ...) -> str:
    """读取文件的实际逻辑"""
    with open(path) as f:
        return f.read()

# ── 模块加载时自动注册 ──
registry.register(
    name="read_file",
    toolset="file",
    schema={...},         # OpenAI tool schema
    handler=read_file_handler,
    check_fn=some_check,  # 可选：可用性检查
)
```

**为什么这样设计可行？**

`model_tools.py` 在启动时调用 `discover_builtin_tools()`，它用 `importlib` 动态 import `tools/` 目录下所有 `.py` 文件。每个文件 import 时执行顶层的 `registry.register(...)` 调用——注册就完成了。

**这叫"自注册模式"（Self-Registration）**，好处：
- 新增工具只需新建文件，不需要改任何调用方代码
- 工具的 Schema 和 Handler 在同一个文件里， locality 好

**找到 `discover_builtin_tools`（line 56-73）：**

```python
def discover_builtin_tools(tools_dir=None) -> List[str]:
    tools_path = Path(tools_dir) if tools_dir else Path(__file__).resolve().parent
    module_names = [
        f"tools.{path.stem}"
        for path in sorted(tools_path.glob("*.py"))
        if path.name not in {"__init__.py", "registry.py", "mcp_tool.py"}
        and _module_registers_tools(path)  # 用 AST 检查是否有 register 调用
    ]
    # 动态 import 触发注册
    for mod_name in module_names:
        importlib.import_module(mod_name)
```

**为什么用 AST 而不是简单的 import？**

防止 import 一个不相关的工具文件（如 `mcp_tool.py` 也是 `.py` 但不应该被当作工具模块）导致误注册。AST 检查确保只有真正调用了 `registry.register()` 的模块才被 import。

---

### Q7：`handle_function_call` 怎么找到对应的工具并执行？

**思考**：如果工具执行抛出异常，Agent Loop 会崩溃吗？

---

**答案**

在 `model_tools.py` 里：

```python
def handle_function_call(function_name, function_args, task_id=None, user_task=None):
    # 1. 查找工具
    entry = registry.get(function_name)
    if not entry:
        return json.dumps({"error": f"unknown tool: {function_name}"})

    # 2. 参数校验（可选）
    if entry.check_fn and not entry.check_fn():
        return json.dumps({"error": f"tool {function_name} not available"})

    # 3. 执行（try/except 保护，不崩溃）
    try:
        result = entry.handler(**function_args, context=...)
        return result if isinstance(result, str) else json.dumps(result)
    except SomeSpecificError as e:
        return json.dumps({"error": f"permission denied: {e}"})
    except Exception as e:
        logger.exception(f"tool {function_name} failed")
        return json.dumps({"error": f"tool failed: {e}"})
```

**关键设计**：异常被捕获，格式化成 JSON 字符串返回——**Agent Loop 永远不会因为工具异常而崩溃**。LLM 会看到错误消息，可以选择重试、换工具、或告诉用户做不到。

---

## agent/prompt_builder.py：Prompt 构建

### Q8：SOUL.md、AGENTS.md、USER.md 分别在什么时机被加载？

**思考**：每次 `run_conversation` 都会重新读文件吗？性能如何？

---

**答案**

**System Prompt 构建只发生一次**，之后缓存：

```python
# run_agent.py line 8685-8723
if self._cached_system_prompt is None:      # ← 第一次才构建
    if stored_prompt:
        # 已有会话：从 SQLite 恢复（保持 Anthropic cache 一致性）
        self._cached_system_prompt = stored_prompt
    else:
        # 新会话：从文件构建
        self._cached_system_prompt = self._build_system_prompt(system_message)
        # 保存到 SQLite
        self._session_db.update_system_prompt(self.session_id, ...)
```

**为什么从 SQLite 恢复而不是每次重新构建？**

因为 Anthropic 的 [Prompt Caching](#q14-anthropic-prompt-caching-是什么) 要求**每次请求的 system prompt 完全相同**才能命中缓存。如果每次从文件读取，即使文件没变，Python 对象到字符串的转换过程中可能有细微差异（空格、换行符），导致 cache miss。

**文件变化时如何失效缓存？**

文件被修改时，Agent 会检测到（通过 mtime 或显式刷新指令），主动清空 `_cached_system_prompt`，下次自动重建。

---

## agent/context_compressor.py：上下文压缩

### Q9：压缩在什么时候触发？压缩的是哪些消息？

**思考**：压缩会不会丢失重要上下文？

---

**答案**

**触发条件**（`run_agent.py` line 8733-8766）：

```python
# 预检压缩：消息总 token 数超过阈值时触发
if (len(messages) > protect_first_n + protect_last_n + 1
        and estimated_tokens >= threshold_tokens):
    messages, system_prompt = self._compress_context(messages, ...)
```

**保护策略**：

```
[第1条] [第2条] ... [中间的消息会被压缩] ... [最近N条] [第1条]
        ↑                                    ↑
     保护前N条                              保护后N条
     （通常是 system/summary）              （最新对话，最重要）
```

**中间消息被压缩成一段摘要**：

```python
# _compress_context 内部：
# 1. 提取"中间"消息（去掉首尾保护段）
middle_messages = messages[protect_first_n : -protect_last_n]
# 2. 调用辅助 LLM（auxiliary client）生成摘要
summary = auxiliary_llm.summarize("\n".join(middle_messages))
# 3. 用摘要替换中间段
compressed = [system_prompt, summary, *last_n_messages]
```

**为什么这样设计？**

- 最近的消息最重要（用户当前在讨论什么）
- 最早的消息可能只是"背景介绍"
- 中间段是历史细节，容易压缩

---

### Q10：压缩失败会发生什么？

**思考**：如果辅助 LLM 也失败了，Agent 会怎样？

---

**答案**

压缩失败时，Hermes 有 fallback 链：

```python
def _compress_context(self, messages, ...):
    try:
        # 尝试用辅助 LLM 压缩
        return self._auxiliary_compress(messages)
    except ContextLengthError:
        # 辅助 LLM 自己的 context 太小 → 降级到简单截断
        return self._truncate_messages(messages)
    except Exception as e:
        # 其他错误 → 记日志，继续（不阻塞对话）
        logger.warning("compression failed: %s", e)
        return messages, system_prompt   # 不压缩，直接返回
```

**第三种 fallback 是关键**：压缩失败不等于对话失败。Hermes 允许"带风险继续"——对话可以继续，只是可能在后续 API 调用时因为 context 超限而失败。

---

## model_tools.py：工具编排

### Q11：`get_tool_definitions` 返回什么？工具 Schema 是什么样？

**思考**：为什么 Schema 要是 OpenAI 兼容的格式？

---

**答案**

```python
def get_tool_definitions(enabled_toolsets=None, disabled_toolsets=None, quiet_mode=False):
    # 从 registry 收集所有已注册工具
    tools = registry.list_tools()

    # 按 toolset 过滤（如果指定了 enabled/disabled）
    if enabled_toolsets:
        tools = [t for t in tools if t.toolset in enabled_toolsets]
    if disabled_toolsets:
        tools = [t for t in tools if t.toolset not in disabled_toolsets]

    # 返回 OpenAI tool calling 格式
    return [t.to_openai_schema() for t in tools]
```

**Schema 格式（OpenAI 兼容）**：

```python
{
    "type": "function",
    "function": {
        "name": "read_file",
        "description": "读取文件内容。适用于...场景",
        "parameters": {
            "type": "object",
            "properties": {
                "path": {
                    "type": "string",
                    "description": "文件路径，绝对或相对路径"
                },
                "limit": {
                    "type": "integer",
                    "description": "最多读取行数（可选）"
                }
            },
            "required": ["path"]
        }
    }
}
```

**为什么用 OpenAI 格式？**

因为 `model_tools.py` 实际上是通过 OpenAI-compatible API 调用的（`client.chat.completions.create`）。统一格式意味着：只要是 OpenAI-compatible 的模型（几乎所有模型），Hermes 都能用，不需要为每个模型写适配器。

---

### Q12：异步工具是怎么处理的？为什么要维护"持久 event loop"？

**思考**：为什么不能每次调用 `asyncio.run()`？

---

**答案**

`model_tools.py` line 44-78：

```python
# 每个线程一个持久 loop
_tool_loop_lock = threading.Lock()
_worker_thread_local = threading.local()  # 线程局部存储

def _get_tool_loop():
    global _tool_loop
    with _tool_loop_lock:
        if _tool_loop is None or _tool_loop.is_closed():
            _tool_loop = asyncio.new_event_loop()
        return _tool_loop

def _get_worker_loop():
    loop = getattr(_worker_thread_local, 'loop', None)
    if loop is None or loop.is_closed():
        loop = asyncio.new_event_loop()
        _worker_thread_local.loop = loop
    return loop
```

**问题背景**：

```
asyncio.run() 会：创建 loop → 执行协程 → 关闭 loop
下次再调用时：创建新 loop → 执行协程 → 关闭 loop

问题：httpx/AsyncOpenAI 客户端绑定了旧的 closed loop
      → GC 时触发 "Event loop is closed" 错误
```

**解决方案**：
- 主线程：全局 `_tool_loop`，永不平仓
- Worker 线程：线程局部存储，每个线程一个 loop
- 协程用 `asyncio.run()` 改用 `loop.run_until_complete()` 在已有 loop 上执行

---

## 错误处理与容错

### Q13：`_repair_tool_call_arguments` 在修什么？为什么需要"修复"？

**思考**：LLM 输出的 JSON 应该总是合法的，但 Hermes 为什么还要修？

---

**答案**

有些模型（尤其是通过 Ollama 跑的 GLM-5.1）会输出**不规范的 JSON**：

```python
# 真实案例：
{"path": "/some/file",}       # 尾随逗号
{"path": "/some/file"         # 未闭合的大括号
{"path": None}                # Python None 而非 null
```

`_repair_tool_call_arguments`（line 494-550）专门修复这些：

```python
def _repair_tool_call_arguments(raw_args: str, tool_name: str = "?") -> str:
    # 1. 空格清理
    raw_args = raw_args.strip()

    # 2. Python None → {}
    if raw_args == "None":
        return "{}"

    # 3. 去除尾随逗号
    fixed = re.sub(r',\s*([}\]])', r'\1', fixed)

    # 4. 补全未闭合的结构
    open_curly = fixed.count('{') - fixed.count('}')
    fixed += '}' * open_curly

    # 5. 去除多余的闭合括号（最多 50 次）
    for _ in range(50):
        try:
            json.loads(fixed)
            return fixed
        except:
            fixed = fixed[:-1]

    # 6. 完全失败 → 返回 {}（避免 400 错误）
    return "{}"
```

**设计哲学**：让 Agent 更健壮。LLM 输出错误 JSON 是常见问题（尤其是非 GPT 系列模型），修复比拒绝更用户体验更好。

---

### Q14：Anthropic Prompt Caching 是什么？代码里怎么体现？

**思考**：为什么缓存 system prompt 能省钱？

---

**答案**

在 `agent/prompt_caching.py` 里，`apply_anthropic_cache_control` 函数：

```python
def apply_anthropic_cache_control(messages: list, system_prompt: str):
    """在 system message 上标记缓存边界"""
    for msg in messages:
        if msg["role"] == "system":
            # 标记缓存控制指令
            msg["cache_control"] = {"type": "ephemeral"}
            break
    return messages
```

**API 层面的效果**：

```
请求1: [system(cache_control=ephemeral) + history] → 缓存 system
请求2: [cache_hit + history + new_turn]           → system 复用，只付新增部分的费用
请求3: [cache_hit + history + new_turn]           → 同上
```

Anthropic 对缓存命中的请求**只收新增 tokens 的费用**，system prompt 通常 4-8KB，100 次请求能省下约 80% 的 system prompt 传输费用。

**缓存失效条件**：
- `system_prompt` 内容变化（所以 Hermes 把 system prompt 缓存在 SQLite 里，每次重建时用完全相同的内容）
- `max_tokens` 变化
- 模型变化

---

### Q15：`_is_destructive_command` 是怎么工作的？为什么需要它？

**思考**：为什么 Terminal 工具不能直接执行 `rm -rf /`？

---

**答案**

`run_agent.py` line 272-296：

```python
_DESTRUCTIVE_PATTERNS = re.compile(
    r"""(?:^|\s|&&|\|\||;|`)(?:
        rm\s|rmdir\s|
        mv\s|
        sed\s+-i|
        truncate\s|
        dd\s|
        shred\s|
        git\s+(?:reset|clean|checkout)\s
    )""",
    re.VERBOSE,
)

def _is_destructive_command(cmd: str) -> bool:
    if _DESTRUCTIVE_PATTERNS.search(cmd):
        return True
    # 覆盖重定向也算（echo > file 会覆盖）
    if _REDIRECT_OVERWRITE.search(cmd):
        return True
    return False
```

**用途**：在执行 Terminal 工具前检查命令是否危险。如果是：

```python
if _is_destructive_command(command):
    # 暂停执行，触发用户审批（clarify_callback）
    # 或者直接拒绝并返回警告
```

**为什么不直接拦截？**

因为有些危险命令是合法的（如 `git reset HEAD~1` 在某些工作流里是必要的）。所以 Hermes 用"标记 + 审批"而不是"直接拒绝"——给用户选择权。

---

## 总结：源码架构全景图

```
┌─────────────────────────────────────────────────────────┐
│  run_conversation()  主循环                              │
│  ├── iteration_budget.check()  迭代预算检查               │
│  ├── _build_system_prompt()   System Prompt（缓存）      │
│  ├── _compress_context()       上下文压缩（超限时）        │
│  └── ┌──────────────────────────────────────────────┐   │
│       │  while budget.remaining > 0:                │   │
│       │  1. 构建 api_messages                        │   │
│       │  2. _interruptible_api_call() → LLM        │   │
│       │  3. 解析 response                           │   │
│       │  4. 有 tool_call → handle_function_call()  │   │
│       │  5. 无 tool_call → 结束                     │   │
│       └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
         │
         ├── model_tools.py
         │   ├── get_tool_definitions()  → 工具 Schema
         │   └── handle_function_call() → 执行工具
         │
         └── tools/registry.py
             ├── discover_builtin_tools() → 自注册
             └── registry.register()     → 每个工具文件调用

关键设计原则（对应文档 04 的 Q&A）：
- Q2 的循环：iteration_budget 控制上限
- Q5 的压缩：context_compressor + 辅助 LLM 摘要
- Q8 的 prompt 组装：分层拼接 + 缓存到 SQLite
- Q9 的工具注册：自注册模式 + AST 扫描
- Q10 的错误处理：try/except + JSON 格式化返回
- Q11 的 Schema：OpenAI 兼容格式
- Q13 的 JSON 修复：容错而非拒绝
- Q14 的 Prompt Caching：Anthropic 缓存协议
```

---

## 附录：常用调试命令

```bash
# 查找关键函数定义
grep -n "def run_conversation\|def handle_function_call\|def _build_system_prompt" \
    /home/ubuntu/.hermes/hermes-agent/run_agent.py

# 查看工具注册情况
grep -n "registry.register" /home/ubuntu/.hermes/hermes-agent/tools/*.py | head -20

# 查看有哪些工具集（toolsets）
grep -n "TOOLSET\|_HERMES_CORE_TOOLS" /home/ubuntu/.hermes/hermes-agent/toolsets.py | head -20

# 查看 Session 存储逻辑
grep -n "session_id\|SessionStore\|save_message" \
    /home/ubuntu/.hermes/hermes-agent/hermes_state.py | head -20

# 跟踪 tool call 日志
grep -n "tool_call\|handle_function" /home/ubuntu/.hermes/hermes-agent/run_agent.py \
    | grep "logger\|logging" | head -20
```

**下一步**：

- 选择你想深入的系统（如工具执行、Session 存储、上下文压缩）
- 用 `grep -n` 找到关键函数
- 用 `read_file(offset=X, limit=Y)` 精读对应代码段
- 对照文档 04 的 Q&A 思考"这里对应哪个设计问题"